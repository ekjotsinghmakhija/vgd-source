name: Type Check

description: "Run static type checker"

runs:
  using: "composite"
  steps:
    - name: Type Check
      run: nix --extra-experimental-features nix-command --extra-experimental-features flakes develop -c basedpyright .
      shell: bash
